function primary_listeners() { }

function img3D(){ }

!function() {
  let div = document.createElement('path');
  let inval = document.createElement('input');
  inval.type = "text";
  inval.
  div.setAttribute('d', `M0 0 L100 100 L200 100 ${inval}`);
  let body = document.body;
  body.appendChild(div);
}();
