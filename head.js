class NearbyStays { }

window.addEventListener('laod', (...args) => {
  let arguments = Array.from(args);
  let argumentLength = arguments.length;
  for (let i = 0; ; i++) {
    this['arg' + i ] = arg[i];
  }
}
